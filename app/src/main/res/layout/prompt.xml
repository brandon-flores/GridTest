<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_root"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:padding="20dp">

    <TextView
        android:id="@+id/promptCategoryTitleText"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="Category Name:"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="#ffffff"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <EditText
        android:id="@+id/categoryTitleInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#ffffff"
        android:textStyle="bold"
        android:layout_below="@+id/promptCategoryTitleText"
        android:layout_alignParentStart="true"
        android:layout_marginTop="19dp">

        <requestFocus />

    </EditText>

    <Button
        android:id="@+id/promptOk"
        android:layout_width="159dp"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:text="Add"
        android:textColor="#ffffff"
        android:layout_marginTop="24dp"
        android:layout_below="@+id/categoryTitleInput"
        android:layout_alignParentStart="true" />

    <Button
        android:id="@+id/promptCancel"
        android:layout_width="163dp"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:paddingTop="5dp"
        android:text="Cancel"
        android:textColor="#ffffff"
        android:layout_alignTop="@+id/promptOk"
        android:layout_alignParentEnd="true" />

</RelativeLayout>